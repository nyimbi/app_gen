
# coding: utf-8
# AUTOGENERATED BY appgen
# Copyright (C) Nyimbi Odero, 2024 


 
import graphene
from graphene_sqlalchemy import SQLAlchemyObjectType, SQLAlchemyConnectionField
from graphene import relay
from flask_graphql import GraphQLView

from flask_appbuilder.security.sqla.models import User, Role, Permission, PermissionView, RegisterUser
from .models import *
from . import app

t_language_proficiency_level_gql = graphene.Enum.from_enum(t_language_proficiency_level)
t_org_type_gql = graphene.Enum.from_enum(t_org_type)
t_organization_status_gql = graphene.Enum.from_enum(t_organization_status)
t_contact_status_gql = graphene.Enum.from_enum(t_contact_status)
t_doc_type_gql = graphene.Enum.from_enum(t_doc_type)
t_doc_status_gql = graphene.Enum.from_enum(t_doc_status)
t_doc_status_gql = graphene.Enum.from_enum(t_doc_status)
t_grant_status_gql = graphene.Enum.from_enum(t_grant_status)
t_climate_category_gql = graphene.Enum.from_enum(t_climate_category)
t_contact_status_gql = graphene.Enum.from_enum(t_contact_status)
t_doc_type_gql = graphene.Enum.from_enum(t_doc_type)
t_org_hierarchy_type_gql = graphene.Enum.from_enum(t_org_hierarchy_type)
t_sdgs_gql = graphene.Enum.from_enum(t_sdgs)
t_doc_verification_status_gql = graphene.Enum.from_enum(t_doc_verification_status)
t_person_role_gql = graphene.Enum.from_enum(t_person_role)
t_person_application_status_gql = graphene.Enum.from_enum(t_person_application_status)
t_project_status_gql = graphene.Enum.from_enum(t_project_status)
t_social_media_platform_gql = graphene.Enum.from_enum(t_social_media_platform)


class BadgeGql(SQLAlchemyObjectType):
    class Meta:
        model = Badge
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class ContactTypeGql(SQLAlchemyObjectType):
    class Meta:
        model = ContactType
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class CurrencyGql(SQLAlchemyObjectType):
    class Meta:
        model = Currency
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class FeaturecodesGql(SQLAlchemyObjectType):
    class Meta:
        model = Featurecodes
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class GeonameGql(SQLAlchemyObjectType):
    class Meta:
        model = Geoname
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class LanguagecodesGql(SQLAlchemyObjectType):
    class Meta:
        model = Languagecodes
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonGql(SQLAlchemyObjectType):
    class Meta:
        model = Person
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class SkillCategoryGql(SQLAlchemyObjectType):
    class Meta:
        model = SkillCategory
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class TagGql(SQLAlchemyObjectType):
    class Meta:
        model = Tag
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class AlternatenameGql(SQLAlchemyObjectType):
    class Meta:
        model = Alternatename
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class ContactGql(SQLAlchemyObjectType):
    class Meta:
        model = Contact
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class CountryGql(SQLAlchemyObjectType):
    class Meta:
        model = Country
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class GamificationChallengeGql(SQLAlchemyObjectType):
    class Meta:
        model = GamificationChallenge
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class LeaderboardGql(SQLAlchemyObjectType):
    class Meta:
        model = Leaderboard
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class MessageGql(SQLAlchemyObjectType):
    class Meta:
        model = Message
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class NotificationGql(SQLAlchemyObjectType):
    class Meta:
        model = Notification
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonBadgeGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonBadge
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonCertificationGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonCertification
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonCourseGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonCourse
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonEducationGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonEducation
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonExperienceGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonExperience
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonHonorAwardGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonHonorAward
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonLanguageGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonLanguage
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    proficiency = graphene.Field(t_language_proficiency_level_gql)

class PersonOrganizationMembershipGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonOrganizationMembership
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonPatentGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonPatent
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonProjectGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonProject
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonPublicationGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonPublication
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonVolunteerExperienceGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonVolunteerExperience
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PointEarningActivityGql(SQLAlchemyObjectType):
    class Meta:
        model = PointEarningActivity
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class ProfileUpdateReminderGql(SQLAlchemyObjectType):
    class Meta:
        model = ProfileUpdateReminder
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class SkillGql(SQLAlchemyObjectType):
    class Meta:
        model = Skill
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class UserActivityGql(SQLAlchemyObjectType):
    class Meta:
        model = UserActivity
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class UserGamificationGql(SQLAlchemyObjectType):
    class Meta:
        model = UserGamification
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class Admin1codesGql(SQLAlchemyObjectType):
    class Meta:
        model = Admin1codes
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class Admin2codesGql(SQLAlchemyObjectType):
    class Meta:
        model = Admin2codes
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class OrganizationGql(SQLAlchemyObjectType):
    class Meta:
        model = Organization
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    org_type = graphene.Field(t_org_type_gql)
    status = graphene.Field(t_organization_status_gql)

class PersonSkillGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonSkill
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class TimezoneGql(SQLAlchemyObjectType):
    class Meta:
        model = Timezone
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class UserChallengeGql(SQLAlchemyObjectType):
    class Meta:
        model = UserChallenge
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class BoardMemberGql(SQLAlchemyObjectType):
    class Meta:
        model = BoardMember
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class ContactApplicationGql(SQLAlchemyObjectType):
    class Meta:
        model = ContactApplication
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    status = graphene.Field(t_contact_status_gql)

class DocumentSubmissionGql(SQLAlchemyObjectType):
    class Meta:
        model = DocumentSubmission
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    document_type = graphene.Field(t_doc_type_gql)
    status = graphene.Field(t_doc_status_gql)
    next_status = graphene.Field(t_doc_status_gql)

class EventGql(SQLAlchemyObjectType):
    class Meta:
        model = Event
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class ExecutivePositionGql(SQLAlchemyObjectType):
    class Meta:
        model = ExecutivePosition
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class GrantGql(SQLAlchemyObjectType):
    class Meta:
        model = Grant
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    status = graphene.Field(t_grant_status_gql)

class OnboardingProgressGql(SQLAlchemyObjectType):
    class Meta:
        model = OnboardingProgress
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class OrganizationAwardGql(SQLAlchemyObjectType):
    class Meta:
        model = OrganizationAward
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class OrganizationBadgeGql(SQLAlchemyObjectType):
    class Meta:
        model = OrganizationBadge
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class OrganizationClimateCategoriesGql(SQLAlchemyObjectType):
    class Meta:
        model = OrganizationClimateCategories
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    climate_category = graphene.Field(t_climate_category_gql)

class OrganizationContactGql(SQLAlchemyObjectType):
    class Meta:
        model = OrganizationContact
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    status = graphene.Field(t_contact_status_gql)

class OrganizationDocumentsGql(SQLAlchemyObjectType):
    class Meta:
        model = OrganizationDocuments
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    document_type = graphene.Field(t_doc_type_gql)

class OrganizationHierarchyGql(SQLAlchemyObjectType):
    class Meta:
        model = OrganizationHierarchy
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    relationship_type = graphene.Field(t_org_hierarchy_type_gql)

class OrganizationProfileGql(SQLAlchemyObjectType):
    class Meta:
        model = OrganizationProfile
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class OrganizationProgramsGql(SQLAlchemyObjectType):
    class Meta:
        model = OrganizationPrograms
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class OrganizationSdgsGql(SQLAlchemyObjectType):
    class Meta:
        model = OrganizationSdgs
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    sdg = graphene.Field(t_sdgs_gql)

class OrganizationTagGql(SQLAlchemyObjectType):
    class Meta:
        model = OrganizationTag
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class OrganizationVerificationGql(SQLAlchemyObjectType):
    class Meta:
        model = OrganizationVerification
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    verification_status = graphene.Field(t_doc_verification_status_gql)

class PersonOrganizationClaimGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonOrganizationClaim
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    claim_type = graphene.Field(t_person_role_gql)
    status = graphene.Field(t_person_application_status_gql)

class ProjectGql(SQLAlchemyObjectType):
    class Meta:
        model = Project
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    status = graphene.Field(t_project_status_gql)

class ReportGql(SQLAlchemyObjectType):
    class Meta:
        model = Report
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class SocialMediaProfileGql(SQLAlchemyObjectType):
    class Meta:
        model = SocialMediaProfile
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)


    platform = graphene.Field(t_social_media_platform_gql)

class TrainingGql(SQLAlchemyObjectType):
    class Meta:
        model = Training
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class VolunteerLogGql(SQLAlchemyObjectType):
    class Meta:
        model = VolunteerLog
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class EventRegistrationGql(SQLAlchemyObjectType):
    class Meta:
        model = EventRegistration
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class ImpactGql(SQLAlchemyObjectType):
    class Meta:
        model = Impact
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PersonTrainingGql(SQLAlchemyObjectType):
    class Meta:
        model = PersonTraining
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class PjFeedbackGql(SQLAlchemyObjectType):
    class Meta:
        model = PjFeedback
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class ProjectLocationsGql(SQLAlchemyObjectType):
    class Meta:
        model = ProjectLocations
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class ProjectTagGql(SQLAlchemyObjectType):
    class Meta:
        model = ProjectTag
        interfaces = (relay.Node, )
        # use `only_fields` to only expose specific fields ie "name"
        # only_fields = ("name",)
        # use `exclude_fields` to exclude specific fields ie "last_name"
        # exclude_fields = (,)



class Query(graphene.ObjectType):
    node = relay.Node.Field()
    # Allows sorting over multiple Badgecolumns, by default over the primary key
    all_badge = SQLAlchemyConnectionField(BadgeGql.connection) #sort=BadgeGql.sort_argument())
    # Allows sorting over multiple ContactTypecolumns, by default over the primary key
    all_contact_type = SQLAlchemyConnectionField(ContactTypeGql.connection) #sort=ContactTypeGql.sort_argument())
    # Allows sorting over multiple Currencycolumns, by default over the primary key
    all_currency = SQLAlchemyConnectionField(CurrencyGql.connection) #sort=CurrencyGql.sort_argument())
    # Allows sorting over multiple Featurecodescolumns, by default over the primary key
    all_featurecodes = SQLAlchemyConnectionField(FeaturecodesGql.connection) #sort=FeaturecodesGql.sort_argument())
    # Allows sorting over multiple Geonamecolumns, by default over the primary key
    all_geoname = SQLAlchemyConnectionField(GeonameGql.connection) #sort=GeonameGql.sort_argument())
    # Allows sorting over multiple Languagecodescolumns, by default over the primary key
    all_languagecodes = SQLAlchemyConnectionField(LanguagecodesGql.connection) #sort=LanguagecodesGql.sort_argument())
    # Allows sorting over multiple Personcolumns, by default over the primary key
    all_person = SQLAlchemyConnectionField(PersonGql.connection) #sort=PersonGql.sort_argument())
    # Allows sorting over multiple SkillCategorycolumns, by default over the primary key
    all_skill_category = SQLAlchemyConnectionField(SkillCategoryGql.connection) #sort=SkillCategoryGql.sort_argument())
    # Allows sorting over multiple Tagcolumns, by default over the primary key
    all_tag = SQLAlchemyConnectionField(TagGql.connection) #sort=TagGql.sort_argument())
    # Allows sorting over multiple Alternatenamecolumns, by default over the primary key
    all_alternatename = SQLAlchemyConnectionField(AlternatenameGql.connection) #sort=AlternatenameGql.sort_argument())
    # Allows sorting over multiple Contactcolumns, by default over the primary key
    all_contact = SQLAlchemyConnectionField(ContactGql.connection) #sort=ContactGql.sort_argument())
    # Allows sorting over multiple Countrycolumns, by default over the primary key
    all_country = SQLAlchemyConnectionField(CountryGql.connection) #sort=CountryGql.sort_argument())
    # Allows sorting over multiple GamificationChallengecolumns, by default over the primary key
    all_gamification_challenge = SQLAlchemyConnectionField(GamificationChallengeGql.connection) #sort=GamificationChallengeGql.sort_argument())
    # Allows sorting over multiple Leaderboardcolumns, by default over the primary key
    all_leaderboard = SQLAlchemyConnectionField(LeaderboardGql.connection) #sort=LeaderboardGql.sort_argument())
    # Allows sorting over multiple Messagecolumns, by default over the primary key
    all_message = SQLAlchemyConnectionField(MessageGql.connection) #sort=MessageGql.sort_argument())
    # Allows sorting over multiple Notificationcolumns, by default over the primary key
    all_notification = SQLAlchemyConnectionField(NotificationGql.connection) #sort=NotificationGql.sort_argument())
    # Allows sorting over multiple PersonBadgecolumns, by default over the primary key
    all_person_badge = SQLAlchemyConnectionField(PersonBadgeGql.connection) #sort=PersonBadgeGql.sort_argument())
    # Allows sorting over multiple PersonCertificationcolumns, by default over the primary key
    all_person_certification = SQLAlchemyConnectionField(PersonCertificationGql.connection) #sort=PersonCertificationGql.sort_argument())
    # Allows sorting over multiple PersonCoursecolumns, by default over the primary key
    all_person_course = SQLAlchemyConnectionField(PersonCourseGql.connection) #sort=PersonCourseGql.sort_argument())
    # Allows sorting over multiple PersonEducationcolumns, by default over the primary key
    all_person_education = SQLAlchemyConnectionField(PersonEducationGql.connection) #sort=PersonEducationGql.sort_argument())
    # Allows sorting over multiple PersonExperiencecolumns, by default over the primary key
    all_person_experience = SQLAlchemyConnectionField(PersonExperienceGql.connection) #sort=PersonExperienceGql.sort_argument())
    # Allows sorting over multiple PersonHonorAwardcolumns, by default over the primary key
    all_person_honor_award = SQLAlchemyConnectionField(PersonHonorAwardGql.connection) #sort=PersonHonorAwardGql.sort_argument())
    # Allows sorting over multiple PersonLanguagecolumns, by default over the primary key
    all_person_language = SQLAlchemyConnectionField(PersonLanguageGql.connection) #sort=PersonLanguageGql.sort_argument())
    # Allows sorting over multiple PersonOrganizationMembershipcolumns, by default over the primary key
    all_person_organization_membership = SQLAlchemyConnectionField(PersonOrganizationMembershipGql.connection) #sort=PersonOrganizationMembershipGql.sort_argument())
    # Allows sorting over multiple PersonPatentcolumns, by default over the primary key
    all_person_patent = SQLAlchemyConnectionField(PersonPatentGql.connection) #sort=PersonPatentGql.sort_argument())
    # Allows sorting over multiple PersonProjectcolumns, by default over the primary key
    all_person_project = SQLAlchemyConnectionField(PersonProjectGql.connection) #sort=PersonProjectGql.sort_argument())
    # Allows sorting over multiple PersonPublicationcolumns, by default over the primary key
    all_person_publication = SQLAlchemyConnectionField(PersonPublicationGql.connection) #sort=PersonPublicationGql.sort_argument())
    # Allows sorting over multiple PersonVolunteerExperiencecolumns, by default over the primary key
    all_person_volunteer_experience = SQLAlchemyConnectionField(PersonVolunteerExperienceGql.connection) #sort=PersonVolunteerExperienceGql.sort_argument())
    # Allows sorting over multiple PointEarningActivitycolumns, by default over the primary key
    all_point_earning_activity = SQLAlchemyConnectionField(PointEarningActivityGql.connection) #sort=PointEarningActivityGql.sort_argument())
    # Allows sorting over multiple ProfileUpdateRemindercolumns, by default over the primary key
    all_profile_update_reminder = SQLAlchemyConnectionField(ProfileUpdateReminderGql.connection) #sort=ProfileUpdateReminderGql.sort_argument())
    # Allows sorting over multiple Skillcolumns, by default over the primary key
    all_skill = SQLAlchemyConnectionField(SkillGql.connection) #sort=SkillGql.sort_argument())
    # Allows sorting over multiple UserActivitycolumns, by default over the primary key
    all_user_activity = SQLAlchemyConnectionField(UserActivityGql.connection) #sort=UserActivityGql.sort_argument())
    # Allows sorting over multiple UserGamificationcolumns, by default over the primary key
    all_user_gamification = SQLAlchemyConnectionField(UserGamificationGql.connection) #sort=UserGamificationGql.sort_argument())
    # Allows sorting over multiple Admin1codescolumns, by default over the primary key
    all_admin1codes = SQLAlchemyConnectionField(Admin1codesGql.connection) #sort=Admin1codesGql.sort_argument())
    # Allows sorting over multiple Admin2codescolumns, by default over the primary key
    all_admin2codes = SQLAlchemyConnectionField(Admin2codesGql.connection) #sort=Admin2codesGql.sort_argument())
    # Allows sorting over multiple Organizationcolumns, by default over the primary key
    all_organization = SQLAlchemyConnectionField(OrganizationGql.connection) #sort=OrganizationGql.sort_argument())
    # Allows sorting over multiple PersonSkillcolumns, by default over the primary key
    all_person_skill = SQLAlchemyConnectionField(PersonSkillGql.connection) #sort=PersonSkillGql.sort_argument())
    # Allows sorting over multiple Timezonecolumns, by default over the primary key
    all_timezone = SQLAlchemyConnectionField(TimezoneGql.connection) #sort=TimezoneGql.sort_argument())
    # Allows sorting over multiple UserChallengecolumns, by default over the primary key
    all_user_challenge = SQLAlchemyConnectionField(UserChallengeGql.connection) #sort=UserChallengeGql.sort_argument())
    # Allows sorting over multiple BoardMembercolumns, by default over the primary key
    all_board_member = SQLAlchemyConnectionField(BoardMemberGql.connection) #sort=BoardMemberGql.sort_argument())
    # Allows sorting over multiple ContactApplicationcolumns, by default over the primary key
    all_contact_application = SQLAlchemyConnectionField(ContactApplicationGql.connection) #sort=ContactApplicationGql.sort_argument())
    # Allows sorting over multiple DocumentSubmissioncolumns, by default over the primary key
    all_document_submission = SQLAlchemyConnectionField(DocumentSubmissionGql.connection) #sort=DocumentSubmissionGql.sort_argument())
    # Allows sorting over multiple Eventcolumns, by default over the primary key
    all_event = SQLAlchemyConnectionField(EventGql.connection) #sort=EventGql.sort_argument())
    # Allows sorting over multiple ExecutivePositioncolumns, by default over the primary key
    all_executive_position = SQLAlchemyConnectionField(ExecutivePositionGql.connection) #sort=ExecutivePositionGql.sort_argument())
    # Allows sorting over multiple Grantcolumns, by default over the primary key
    all_grant = SQLAlchemyConnectionField(GrantGql.connection) #sort=GrantGql.sort_argument())
    # Allows sorting over multiple OnboardingProgresscolumns, by default over the primary key
    all_onboarding_progress = SQLAlchemyConnectionField(OnboardingProgressGql.connection) #sort=OnboardingProgressGql.sort_argument())
    # Allows sorting over multiple OrganizationAwardcolumns, by default over the primary key
    all_organization_award = SQLAlchemyConnectionField(OrganizationAwardGql.connection) #sort=OrganizationAwardGql.sort_argument())
    # Allows sorting over multiple OrganizationBadgecolumns, by default over the primary key
    all_organization_badge = SQLAlchemyConnectionField(OrganizationBadgeGql.connection) #sort=OrganizationBadgeGql.sort_argument())
    # Allows sorting over multiple OrganizationClimateCategoriescolumns, by default over the primary key
    all_organization_climate_categories = SQLAlchemyConnectionField(OrganizationClimateCategoriesGql.connection) #sort=OrganizationClimateCategoriesGql.sort_argument())
    # Allows sorting over multiple OrganizationContactcolumns, by default over the primary key
    all_organization_contact = SQLAlchemyConnectionField(OrganizationContactGql.connection) #sort=OrganizationContactGql.sort_argument())
    # Allows sorting over multiple OrganizationDocumentscolumns, by default over the primary key
    all_organization_documents = SQLAlchemyConnectionField(OrganizationDocumentsGql.connection) #sort=OrganizationDocumentsGql.sort_argument())
    # Allows sorting over multiple OrganizationHierarchycolumns, by default over the primary key
    all_organization_hierarchy = SQLAlchemyConnectionField(OrganizationHierarchyGql.connection) #sort=OrganizationHierarchyGql.sort_argument())
    # Allows sorting over multiple OrganizationProfilecolumns, by default over the primary key
    all_organization_profile = SQLAlchemyConnectionField(OrganizationProfileGql.connection) #sort=OrganizationProfileGql.sort_argument())
    # Allows sorting over multiple OrganizationProgramscolumns, by default over the primary key
    all_organization_programs = SQLAlchemyConnectionField(OrganizationProgramsGql.connection) #sort=OrganizationProgramsGql.sort_argument())
    # Allows sorting over multiple OrganizationSdgscolumns, by default over the primary key
    all_organization_sdgs = SQLAlchemyConnectionField(OrganizationSdgsGql.connection) #sort=OrganizationSdgsGql.sort_argument())
    # Allows sorting over multiple OrganizationTagcolumns, by default over the primary key
    all_organization_tag = SQLAlchemyConnectionField(OrganizationTagGql.connection) #sort=OrganizationTagGql.sort_argument())
    # Allows sorting over multiple OrganizationVerificationcolumns, by default over the primary key
    all_organization_verification = SQLAlchemyConnectionField(OrganizationVerificationGql.connection) #sort=OrganizationVerificationGql.sort_argument())
    # Allows sorting over multiple PersonOrganizationClaimcolumns, by default over the primary key
    all_person_organization_claim = SQLAlchemyConnectionField(PersonOrganizationClaimGql.connection) #sort=PersonOrganizationClaimGql.sort_argument())
    # Allows sorting over multiple Projectcolumns, by default over the primary key
    all_project = SQLAlchemyConnectionField(ProjectGql.connection) #sort=ProjectGql.sort_argument())
    # Allows sorting over multiple Reportcolumns, by default over the primary key
    all_report = SQLAlchemyConnectionField(ReportGql.connection) #sort=ReportGql.sort_argument())
    # Allows sorting over multiple SocialMediaProfilecolumns, by default over the primary key
    all_social_media_profile = SQLAlchemyConnectionField(SocialMediaProfileGql.connection) #sort=SocialMediaProfileGql.sort_argument())
    # Allows sorting over multiple Trainingcolumns, by default over the primary key
    all_training = SQLAlchemyConnectionField(TrainingGql.connection) #sort=TrainingGql.sort_argument())
    # Allows sorting over multiple VolunteerLogcolumns, by default over the primary key
    all_volunteer_log = SQLAlchemyConnectionField(VolunteerLogGql.connection) #sort=VolunteerLogGql.sort_argument())
    # Allows sorting over multiple EventRegistrationcolumns, by default over the primary key
    all_event_registration = SQLAlchemyConnectionField(EventRegistrationGql.connection) #sort=EventRegistrationGql.sort_argument())
    # Allows sorting over multiple Impactcolumns, by default over the primary key
    all_impact = SQLAlchemyConnectionField(ImpactGql.connection) #sort=ImpactGql.sort_argument())
    # Allows sorting over multiple PersonTrainingcolumns, by default over the primary key
    all_person_training = SQLAlchemyConnectionField(PersonTrainingGql.connection) #sort=PersonTrainingGql.sort_argument())
    # Allows sorting over multiple PjFeedbackcolumns, by default over the primary key
    all_pj_feedback = SQLAlchemyConnectionField(PjFeedbackGql.connection) #sort=PjFeedbackGql.sort_argument())
    # Allows sorting over multiple ProjectLocationscolumns, by default over the primary key
    all_project_locations = SQLAlchemyConnectionField(ProjectLocationsGql.connection) #sort=ProjectLocationsGql.sort_argument())
    # Allows sorting over multiple ProjectTagcolumns, by default over the primary key
    all_project_tag = SQLAlchemyConnectionField(ProjectTagGql.connection) #sort=ProjectTagGql.sort_argument())

schema = graphene.Schema(query=Query)
app.add_url_rule('/graphql', view_func=GraphQLView.as_view(
    'graphql',
    schema=schema,
    graphiql=True,
))
